#include <fstream>
#include <iostream>
#include <regex>
#include <map>

#define REPORT(X) std::cout << (X)

int main()
{
    std::ifstream infile("input.txt");
    std::string line;


    while (std::getline(infile, line))
    {
        
    }

}